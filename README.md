# How-to run
### First time
```npm install```
### To display the page
```npm start```
### To build static HTML/JS/CSS
```npm run build```
Then you can go to ```/build``` directory to access static ```index.html```
